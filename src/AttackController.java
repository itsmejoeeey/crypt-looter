import java.awt.*;

public class AttackController {
    public double rotation;
    private Rectangle rect;

    public Point[] getPoints(){
        Point[] points = new Point[4];
        for (int i= 0; i< 4; i++){
            //epoints[i] = new Point(rect.x * Math.cos(rotation), rect.y * Math.cos(rotation))
        }
        return points;
    }
}
